{
    "version": 2,
    "builds": [
      { "src": "src/**/*", "use": "@vercel/static-build", "config": { "distDir": "dist/angular-project" } }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }
  